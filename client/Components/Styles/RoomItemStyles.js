import { StyleSheet } from 'react-native';
import globalStyles from '../../Config/Styling';

const styles = StyleSheet.create({
  roomHeader: { flexDirection: 'row' },
  icon: {},
});

export { styles };
